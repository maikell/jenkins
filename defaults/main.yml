---
 install_java: true
 jenkins_port: 8080

 # Debian vars
 debian_backports_distribution: "{{ ansible_distribution_release }}"
 debian_jenkins_repo_url: deb https://pkg.jenkins.io/debian binary/
 debian_jenkins_repo_key_url: https://pkg.jenkins.io/debian/jenkins.io.key
 debian_backports_url: http://http.debian.net/debian
 debian_backports_components: "{{ debian_backports_distribution }}-backports main contrib non-free"

 # Centos vars

 centos_jenkins_repo_url:  http://pkg.jenkins.io/redhat
 centos_jenkins_repo_key_url: https://jenkins-ci.org/redhat/jenkins-ci.org.key
 